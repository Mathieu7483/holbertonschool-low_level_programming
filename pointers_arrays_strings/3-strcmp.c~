#include "main.h"
/**
*_strcmp - compare the string
*@s1: source tab
*@s2: dest tab
*Return: value of strcmp
*/
int _strcmp(char *s1, char *s2)
{
if (*s1 == *s2)
return (0);
else if (*s1 > *s2)
return (*s1 - *s2);
else
return (*s2 - *s1);
}
