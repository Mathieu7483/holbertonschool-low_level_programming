#include "main.h"
/**
*_strlen - calculate the length of a string
*@s: string of character
* Return: Lenght of a string
*/
int _strlen(char *s)
{
int lenght = 0;
while (s[lenght])
{
lenght++;
}
return (length);
}
