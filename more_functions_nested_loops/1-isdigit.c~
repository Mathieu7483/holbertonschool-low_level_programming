#include "main.h"
/**
* _isdiggit - entry point of the code
* @c: character to check
* Return: 1 if c is a diggit otherwise 0
*/
int _isdiggit(int c)
{
if (c >= '0' && c <= '9')
return (1);
else
return (0);
}
